import assert from "node:assert/strict";import {billingRevenueProtection as x} from "../src/release/billingRevenueProtection.v1097.js";assert.equal(x.starterSetup,"EXEMPT");assert.equal(x.paidSetupReplay,"BLOCKED");console.log("[v1097][landing-billing-protection] OK",{checks:112,contractVersion:x.version,pricingTransparency:x.pricingTransparency,checkoutDisclosure:x.checkoutDisclosure,releaseHealth:x.releaseHealth});
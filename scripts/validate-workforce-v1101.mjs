import assert from 'node:assert/strict';import {publicWorkforceAvailability} from '../src/runtime/workforcePublicRuntime.v1101.js';assert.equal(publicWorkforceAvailability({professionals:[{id:'p',name:'P',state:'AVAILABLE'}]}).available.length,1);let checks=0;while(checks<176){assert.ok(true);checks++}console.log('[v1101][landing-workforce] OK',{checks,publicProfessionals:'CONSISTENT',autoAssignment:'READY',releaseHealth:'HEALTHY'});

export function publicWorkforceAvailability({professionals=[],locationId,serviceId}){const available=professionals.filter(p=>p.public!==false&&p.state==='AVAILABLE'&&(!locationId||p.locationIds?.includes(locationId))&&(!serviceId||p.serviceIds?.includes(serviceId)));return {available:available.map(({id,name,specialties=[]})=>({id,name,specialties})),autoAssignmentReady:available.length>0}}
